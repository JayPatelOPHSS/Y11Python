#for07
total=0
for i in range(1,6):
    num=int(input(" enter a number"))
    keep=str(input("\n do u want to include that number y=yes, n=no")).lower()
    if keep=="y":
        total=total+num
    elif total=="n":
        total=total

print(total)
    
